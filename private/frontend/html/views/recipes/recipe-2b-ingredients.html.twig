          <div class="mt-3" id="ingredients-anchor">
            <h4>{{Controller.l('recipe_ingredients_header')}}</h4>
            <p></p>
            <ul class="list-group list-group-flush list-group-sm">
              {% for Ingredient in Recipe.getIngredients() %}
                <li class="list-group-item">
                  <div class="d-flex w-100 justify-content-between">
                    <p class="w-30 text-right pr-1 my-1">
                      {{ Ingredient.getQuantity() }}
                      {% if Ingredient.getUnit() %}{{ Ingredient.getUnit().getName() }}{% endif %}
                    </p>
                    <p class="w-70 pl-1 my-1">
                      {{ Ingredient.getDescription() }}
                    </p>
                  </div>
                </li>
              {% endfor %}
            </ul>
          </div>{% if Recipe.getOverallTime() %}
          <div class="mt-3">
            <h4>{{ Controller.l('recipe_duration_header') }}</h4>
            <p>{% if Recipe.getOverallTime() >= Config.LongTimeWarning() %}
              {{ Controller.l('recipe_duration_description') }}{% else %}
              {{ Controller.l('recipe_duration_description2') }}{% endif %}
              <ul>{% if Recipe.getPreparationTime() %}
                <li>{{ Controller.l('recipe_duration_preparation', Recipe.getPreparationTimeStr()) }}</li>{% endif %}{% if Recipe.getCookingTime() %}
                <li>{{ Controller.l('recipe_duration_cooking', Recipe.getCookingTimeStr()) }}</li>{% endif %}{% if Recipe.getRestTime() %}
                <li>{{ Controller.l('recipe_duration_rest', Recipe.getRestTimeStr()) }}</li>{% endif %}
              </ul>
            </p>
          </div>{% endif %}

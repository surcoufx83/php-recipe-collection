
            <b-container fluid
              class="container-max1280 breadcrumbs"
              v-if="page.contentData.hasActions === true || page.contentData.hasFilters === true">
              <page-actions-container
                :page="page" :user="user">
              </page-actions-container>
            </b-container>
